{
  "name": "tikshop-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build:user-app": "cd user-app && npm ci && npm run build",
    "build:merchant": "cd merchant && npm ci && npm run build",
    "build:admin": "cd admin && npm ci && npm run build",
    "build:backend": "cd ecommerce-backend && npm ci && npm run build",
    "dev:user-app": "cd user-app && npm run dev",
    "dev:merchant": "cd merchant && npm run dev",
    "dev:admin": "cd admin && npm run dev",
    "dev:backend": "cd ecommerce-backend && npm run dev",
    "start:backend": "cd ecommerce-backend && npm run start:prod"
  },
  "engines": {
    "node": "^20.19.0 || >=22.12.0"
  }
}